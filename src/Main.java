import java.awt.*;

public class Main {
    public static void main(String[] args) {
        new WelcomeFrame();
        /*GraphicsEnvironment graphicsEnvironment = GraphicsEnvironment.getLocalGraphicsEnvironment();
        GraphicsDevice[] graphicsDevices = graphicsEnvironment.getScreenDevices(); //возвращает массив устройств

        System.out.println("number of devises " +  graphicsDevices.length);

        int i = 0;
        for (GraphicsDevice device : graphicsDevices) {
            GraphicsConfiguration[] configurations = device.getConfigurations();


            System.out.println("number of conf of " + i + " devise: " + configurations.length);
        }*/
    }
}